name: apply

on:
  push:
    branches: [main]
    paths: ['modules/**', 'envs/**', 'provisioning/**']
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to apply
        required: true
        default: dev
        type: choice
        options: [dev, stage, prod]

permissions:
  id-token: write
  contents: read

jobs:
  apply:
    runs-on: ubuntu-latest
    # `stage` and `prod` are GitHub Environments with required reviewers, so
    # the run pauses here until someone approves it.
    environment: ${{ inputs.environment || 'dev' }}
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with: { terraform_version: 1.13.3 }

      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Re-plan
        # Re-plan at apply time rather than applying a stored plan, so drift
        # introduced between review and merge is caught.
        run: |
          ENV=${{ inputs.environment || 'dev' }}
          terraform -chdir=envs/$ENV init
          terraform -chdir=envs/$ENV plan -out=tfplan

      - name: Apply
        run: |
          ENV=${{ inputs.environment || 'dev' }}
          terraform -chdir=envs/$ENV apply -auto-approve tfplan

      - uses: actions/upload-artifact@v4
        with:
          name: apply-log-${{ inputs.environment || 'dev' }}
          path: envs/*/tfplan
